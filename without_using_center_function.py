# define function to center text
def center_text(text, width):
# check text length
# calculate spaces needed
    total_space = max(0,width-len(text))
    right_spaces = total_space - left_spaces
# divide spaces equally
    left_spaces = total_space // 2
# return text
# ask for user input
# print the centered text